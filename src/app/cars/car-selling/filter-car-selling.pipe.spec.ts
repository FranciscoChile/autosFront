import { FilterCarSellingPipe } from './filter-car-selling.pipe';

describe('FilterCarSellingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarSellingPipe();
    expect(pipe).toBeTruthy();
  });
});
